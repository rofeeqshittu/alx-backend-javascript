// Return a promise using prototype

function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
     // The promise is created, but not resolved or rejected yet
  });
}

export default getResponseFromAPI;
